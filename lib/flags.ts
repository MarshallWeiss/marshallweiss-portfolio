/**
 * Personal essays are published.
 *
 * This single switch controls both the "Thinking out loud" section on the
 * homepage and the "Mine" tab on /thoughts, so the two can't drift out of
 * sync. Set it to false to pull the essays back out of the site's navigation
 * without deleting anything.
 */
export const SHOW_PERSONAL_WRITING = true;
